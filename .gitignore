# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Firebase config (keep .env private even though these values aren't secret)
# and the service account key (downloaded for GitHub Actions / the Worker —
# never commit it, wherever it happens to land).
.env
scripts/service-account.json
*firebase-adminsdk*.json
dev-dist
.firebase

# Generated from scripts/firebase-messaging-sw.template.js + .env at build time
public/firebase-messaging-sw.js
